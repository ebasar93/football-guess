// Where the app connects for online matches. Change this to your deployed
// server (e.g. 'wss://football-guess.up.railway.app') — it can also be
// edited in-app on the online lobby screen.
export const DEFAULT_SERVER_URL = 'ws://localhost:8080';
